# TODO

Дорожная карта проекта: что уже сделано и куда движемся. Дополняет `CLAUDE.md` (правила) и `docs/TZ.md` (спецификация).

Легенда: ✅ готово · 🚧 частично / в работе · ⬜ запланировано

---

## ✅ Сделано

### Инфраструктура и монорепо
- ✅ Монорепо на pnpm workspaces + Turborepo, Next.js 15 (App Router) + React 19, TypeScript strict.
- ✅ Общие пресеты в `packages/config` (eslint / tsconfig `base|library|nextjs` / tailwind).
- ✅ Команды `dev` / `build` / `lint` / `typecheck` / `test` через turbo; `predev` убивает процесс на :3000.

### Движок правил (`packages/rules`)
- ✅ Чистые модули формул: `tiers`, `derived`, `combat`, `bubble`, `reputation`, `currency`, `satiety`.
- ✅ Zod-конфиг правил `RuleConfigSchema` + `DEFAULT_RULE_CONFIG`.
- ✅ Юнит-тесты движка: покрыты все модули формул — `tiers`, `bubble`, `combat`, `derived`, `reputation`, `currency`, `satiety` — плюс Zod-конфиг `RuleConfigSchema` (80 кейсов).

### База данных (`packages/db`)
- ✅ Полная Prisma-схема (PostgreSQL): аккаунты/auth, справочники, кампании, персонаж, рантайм, инвентарь, репутация, питомец, врождённая способность, дикая магия, классовые бонусы, аудит.
- ✅ `RuntimeState` с парами `*Computed`/`*Override`/`*ManualOverride` для производных значений.
- ✅ Free-text раса/группировка (`raceName`/`groupName`) рядом с FK.
- ✅ 7 миграций.
- ✅ Сид справочников: `rule-config`, расы, группы, типажи НПС, dev-пользователи.
- ✅ Сид каталога предметов: статические TS-данные `item-templates-defaults.ts` (`ITEM_TEMPLATES`, 112 шаблонов, idempotent upsert по `name`) подключены в дефолтный `pnpm db:seed`. Ранее парсились из `Gob_markets.csv`; данные перенесены в TS ради единообразия и самодостаточного деплоя.
- 🚧 Сиды персонажа и скиллов: демо-персонаж под `SEED_DEMO` переписан заново (`character-demo.ts`); сид каталога скиллов и категорий ещё впереди.

### Авторизация
- ✅ Auth.js / NextAuth v5: Credentials (email+пароль, bcrypt), JWT-сессии, роль в токене.
- ✅ `middleware.ts` редиректит неавторизованных на `/sign-in`.
- ✅ Роли `player` / `gm` / `admin` как область видимости; проверки владения в server actions.

### Веб-интерфейс (`apps/web`)
- ✅ Лист персонажа с вкладками: Описание, Характеристики, Снаряжение, Скиллы, Рюкзак, Репутация, Заметки.
- ✅ Прямое редактирование базовых характеристик владельцем (не только ГМ), режим распределения очков.
- ✅ Диалоги-пикеры: снаряжение, скилл, раса, группировка (с поддержкой free-text).
- ✅ Список и создание персонажей, soft-delete.
- ✅ Панель ГМ: кампании, party-controls.
- ✅ Панель админа: правила (`RuleConfig`-редактор), предметы, расы, группы, скиллы.
- ✅ Настройки профиля (имя, пароль), переключатель темы, переключатель раскладки листа.
- ✅ i18n через i18next (локаль `ru`).

---

## 🎯 Векторы развития

### 1. Автоматизация развёртывания «в один файл» ✅ — приоритет
Цель: на чистом сервере запуск одного скрипта поднимает всё и сразу даёт рабочий сайт.
- ✅ `Dockerfile` для веба (`apps/web/Dockerfile`) — multi-stage (`deps`/`builder`/`migrator`/`runner`), `output: "standalone"` + `outputFileTracingRoot` в `next.config.ts`.
- ✅ `docker-compose.yml`: сервисы `db` (postgres:16-alpine, volume `db_data` + healthcheck), `migrate` (одноразовый) и `web`.
- ✅ Bootstrap-скрипт (`start.sh` / `make up`), который:
  - поднимает контейнер БД; ждёт `healthy` (через `depends_on: service_healthy`);
  - применяет миграции (`prisma migrate deploy`) в сервисе `migrate`;
  - идемпотентно засевает БД (upsert; `web` стартует после `service_completed_successfully`);
  - стартует сервер и печатает URL `http://localhost:3000`.
- ✅ Генерация `AUTH_SECRET` при первом запуске, если не задан; `.env.production.example`.
- ✅ Идемпотентный seed (`upsert` везде) + первичный admin из ENV (`initial-admin.ts`) + флаг `SEED_DEMO` (dev-пользователи).
- ✅ Windows-аналог bootstrap-скрипта: `start.ps1` (+ `start.cmd` для запуска двойным кликом) — повторяет логику `start.sh` на PowerShell, секреты через .NET RNG вместо `openssl`.
- ✅ README-раздел «Деплой одной командой».

### 2. Публичный API + мобильное приложение ⬜
Мобильный клиент — это только фронтенд, общающийся с основным сервером по API. **Предусловие** — вынести мутации из Server Actions в стабильный публичный API.
- ⬜ **Слой API**: REST Route Handlers (`/api/*`) или tRPC поверх существующей логики; единый источник — те же server-функции, без дублирования правил.
- ⬜ **Токен-авторизация для клиентов** (bearer/JWT или OAuth-flow) — текущая cookie-сессия не подходит для нативного клиента.
- ⬜ OpenAPI-схема / типизированный SDK, переиспользующий Zod-схемы.
- ⬜ Версионирование API и CORS-политика.
- ⬜ Мобильный клиент (React Native / Expo или Flutter — выбрать) c теми же экранами листа персонажа; вся игровая математика остаётся на сервере (`@gob/rules`).

### 3. Тесты и CI/CD ⬜
- ✅ Юнит-тесты `packages/rules` исчерпывающие (каждая граница: тиры, классы `{6,9,12,20}`, бабл D100, урон/крит, сытость, валюта, репутация, Zod-конфиг).
- ⬜ Настроить Playwright (конфиг отсутствует, хотя `pnpm test:e2e` объявлен) + смоук E2E ключевых сценариев.
- ⬜ GitHub Actions: `lint` + `typecheck` + `test` на каждый PR; сборка Docker-образа на main.
- ⬜ Интеграционные тесты server actions (права доступа по ролям/владению).

### 4. Заполнение справочников 🚧
- ✅ Сид предметов подключён в основной пайплайн (статические TS-данные `item-templates-defaults.ts`, 112 шаблонов).
- ⬜ Сид каталога скиллов и категорий.
- ✅ Демо-персонаж под флагом `SEED_DEMO` для быстрой проверки UI (`character-demo.ts` → «Бранд Серый», владелец `demo@gob.local`): заполнены все вкладки листа, ручные оверрайды, снаряжение по шаблонам каталога, валюта с транзакциями, питомец, врождёнка, эффекты; идемпотентен.

### 5. Загрузка изображений (S3/R2) ⬜
- ⬜ Presigned-URL upload для `appearanceImage`, иконок предметов/скиллов/питомца (сейчас это просто строки).
- ⬜ Заполнить переменные `S3_*` и описать настройку в README.

### 6. Недостающие игровые подсистемы в UI ⬜
Модели в БД готовы, экранов пока нет:
- ⬜ Питомец (`Pet`): уровень, прогресс кормёжки, бонусы, способность.
- ⬜ Врождённая способность (`InnateAbility`) и её ранги.
- ⬜ Дикая магия (`WildMagicCard` / `WildMagicDraw`): вытягивание/выбор карт.
- ⬜ Классовые бонусы (`ClassBonusRecord`): пороги, бросок кубов, фиксация эффекта.
- ⬜ Валюта и история транзакций (`Currency` / `CurrencyTransaction`) — экран баланса с обязательным `moneyTarget`.
- ⬜ Просмотр аудит-лога (`AuditLog`).
- ⬜ Бросок кубов: формулы боя уже принимают готовые значения дайсов — добавить роллер + историю бросков.

### 7. PWA и офлайн ⬜
- ⬜ Полноценный PWA (Serwist/next-pwa): `manifest.json`, service worker (сейчас есть только `manifest` в метаданных).
- ⬜ Клиентское кэширование справочников для офлайн-доступа (заложено в правилах проекта).

### 8. Прод-эксплуатация и наблюдаемость ⬜
- ⬜ Бэкап БД (cron) и стратегия восстановления.
- ⬜ Логирование ошибок (Sentry или аналог), базовые метрики.
- ⬜ Rate limiting на публичные эндпоинты после появления API.

---

## 💡 Идеи к обсуждению
- Экспорт/импорт персонажа (JSON) и шаринг листа по ссылке (read-only).
- Англоязычная локаль (`en`) — структура i18n уже готова.
- Realtime-синхронизация листа во время игры (WebSocket/SSE) — несколько устройств за столом.
- Калькулятор боя на основе `@gob/rules` как отдельный режим для ГМ.
