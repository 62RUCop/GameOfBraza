# TODO

Дорожная карта проекта: что уже сделано и куда движемся. Дополняет `CLAUDE.md` (правила) и `docs/TZ.md` (спецификация).

Легенда: ✅ готово · 🚧 частично / в работе · ⬜ запланировано

---

## ✅ Сделано

### Инфраструктура и монорепо
- ✅ Монорепо на pnpm workspaces + Turborepo, Next.js 15 (App Router) + React 19, TypeScript strict.
- ✅ Общие пресеты в `packages/config` (eslint / tsconfig `base|library|nextjs` / tailwind).
- ✅ Команды `dev` / `build` / `lint` / `typecheck` / `test` через turbo; `predev` убивает процесс на :3000.

### Движок правил (`packages/rules`)
- ✅ Чистые модули формул: `tiers`, `derived`, `combat`, `bubble`, `reputation`, `currency`, `satiety`.
- ✅ Zod-конфиг правил `RuleConfigSchema` + `DEFAULT_RULE_CONFIG`.
- ✅ Юнит-тесты движка: покрыты все модули формул — `tiers`, `bubble`, `combat`, `derived`, `dice`, `reputation`, `currency`, `satiety` — плюс Zod-конфиг `RuleConfigSchema` (106 кейсов).

### База данных (`packages/db`)
- ✅ Полная Prisma-схема (PostgreSQL): аккаунты/auth, справочники, кампании, персонаж, рантайм, инвентарь, репутация, питомец, врождённая способность, дикая магия, классовые бонусы, аудит.
- ✅ `RuntimeState` с парами `*Computed`/`*Override`/`*ManualOverride` для производных значений.
- ✅ Free-text раса/группировка (`raceName`/`groupName`) рядом с FK.
- ✅ 7 миграций.
- ✅ Сид справочников: `rule-config`, расы, группы, типажи НПС, dev-пользователи.
- ✅ Сид каталога предметов: статические TS-данные `item-templates-defaults.ts` (`ITEM_TEMPLATES`, 112 шаблонов, idempotent upsert по `name`) подключены в дефолтный `pnpm db:seed`. Ранее парсились из `Gob_markets.csv`; данные перенесены в TS ради единообразия и самодостаточного деплоя.
- 🚧 Сиды персонажа и скиллов: демо-персонаж под `SEED_DEMO` переписан заново (`character-demo.ts`); сид каталога скиллов и категорий ещё впереди.

### Авторизация
- ✅ Auth.js / NextAuth v5: Credentials (email+пароль, bcrypt), JWT-сессии, роль в токене.
- ✅ `middleware.ts` редиректит неавторизованных на `/sign-in`.
- ✅ Роли `player` / `gm` / `admin` как область видимости; проверки владения в server actions.

### Веб-интерфейс (`apps/web`)
- ✅ Лист персонажа с вкладками: Описание, Характеристики, Снаряжение, Скиллы, Рюкзак, Репутация, Заметки.
- ✅ Прямое редактирование базовых характеристик владельцем (не только ГМ), режим распределения очков.
- ✅ Диалоги-пикеры: снаряжение, скилл, раса, группировка (с поддержкой free-text).
- ✅ Список и создание персонажей, soft-delete.
- ✅ Панель ГМ: кампании, party-controls.
- ✅ Панель админа: правила (`RuleConfig`-редактор), предметы, расы, группы, скиллы.
- ✅ Настройки профиля (имя, пароль), переключатель темы, переключатель раскладки листа.
- ✅ i18n через i18next (локаль `ru`).

---

## 🎯 Векторы развития

### 0. Баги и исправления
- ✅ **Ячейки способностей** Максимум ячеек скиллов теперь переносится из вкладки характеристик на вкладку скиллов: `tab-skills` считает лимит через `computeDerived` (`@gob/rules`) и уважает ручной оверрайд `slotsManualOverride`/`slotsOverride` из `RuntimeState`.
- ✅ **Коэффиценты валют** silverToBronze и goldToSilver теперь работают на вкладке Рюкзак: реальный `RuleConfig` прокидывается в `tab-backpack` (`formatCurrency`/`toBronze`) вместо захардкоженного `DEFAULT_RULE_CONFIG`.

### 1. Автоматизация развёртывания «в один файл» ✅ — приоритет
Цель: на чистом сервере запуск одного скрипта поднимает всё и сразу даёт рабочий сайт.
- ✅ `Dockerfile` для веба (`apps/web/Dockerfile`) — multi-stage (`deps`/`builder`/`migrator`/`runner`), `output: "standalone"` + `outputFileTracingRoot` в `next.config.ts`.
- ✅ `docker-compose.yml`: сервисы `db` (postgres:16-alpine, volume `db_data` + healthcheck), `migrate` (одноразовый) и `web`.
- ✅ Bootstrap-скрипт (`start.sh` / `make up`), который:
  - поднимает контейнер БД; ждёт `healthy` (через `depends_on: service_healthy`);
  - применяет миграции (`prisma migrate deploy`) в сервисе `migrate`;
  - идемпотентно засевает БД (upsert; `web` стартует после `service_completed_successfully`);
  - стартует сервер и печатает URL `http://localhost:3000`.
- ✅ Генерация `AUTH_SECRET` при первом запуске, если не задан; `.env.production.example`.
- ✅ Идемпотентный seed (`upsert` везде) + первичный admin из ENV (`initial-admin.ts`) + флаг `SEED_DEMO` (dev-пользователи).
- ✅ Windows-аналог bootstrap-скрипта: `start.ps1` (+ `start.cmd` для запуска двойным кликом) — повторяет логику `start.sh` на PowerShell, секреты через .NET RNG вместо `openssl`.
- ✅ README-раздел «Деплой одной командой».

### 2. Публичный API + мобильное приложение ⬜
Мобильный клиент — это только фронтенд, общающийся с основным сервером по API. **Предусловие** — вынести мутации из Server Actions в стабильный публичный API.
- ⬜ **Слой API**: REST Route Handlers (`/api/*`) или tRPC поверх существующей логики; единый источник — те же server-функции, без дублирования правил.
- ⬜ **Токен-авторизация для клиентов** (bearer/JWT или OAuth-flow) — текущая cookie-сессия не подходит для нативного клиента.
- ⬜ OpenAPI-схема / типизированный SDK, переиспользующий Zod-схемы.
- ⬜ Версионирование API и CORS-политика.
- ⬜ Мобильный клиент (React Native / Expo или Flutter — выбрать) c теми же экранами листа персонажа; вся игровая математика остаётся на сервере (`@gob/rules`).

### 3. Тесты и CI/CD ⬜
- ✅ Юнит-тесты `packages/rules` исчерпывающие (каждая граница: тиры, классы `{6,9,12,20}`, бабл D100, урон/крит, сытость, валюта, репутация, Zod-конфиг).
- ⬜ Настроить Playwright (конфиг отсутствует, хотя `pnpm test:e2e` объявлен) + смоук E2E ключевых сценариев.
- ⬜ GitHub Actions: `lint` + `typecheck` + `test` на каждый PR; сборка Docker-образа на main.
- ⬜ Интеграционные тесты server actions (права доступа по ролям/владению).

### 4. Заполнение справочников 🚧
- ✅ Сид предметов подключён в основной пайплайн (статические TS-данные `item-templates-defaults.ts`, 112 шаблонов).
- ⬜ Сид каталога скиллов и категорий.
- ✅ Демо-персонаж под флагом `SEED_DEMO` для быстрой проверки UI (`character-demo.ts` → «Бранд Серый», владелец `demo@gob.local`): заполнены все вкладки листа, ручные оверрайды, снаряжение по шаблонам каталога, валюта с транзакциями, питомец, врождёнка, эффекты; идемпотентен.

### 5. Загрузка изображений (S3/R2) ⬜
- ⬜ Presigned-URL upload для `appearanceImage`, иконок предметов/скиллов/питомца (сейчас это просто строки).
- ⬜ Заполнить переменные `S3_*` и описать настройку в README.

### 6. Недостающие игровые подсистемы в UI ⬜
Модели в БД готовы, экранов пока нет:
- ⬜ Питомец (`Pet`): уровень, прогресс кормёжки, бонусы, способность.
- ⬜ Врождённая способность (`InnateAbility`) и её ранги.
- ⬜ Дикая магия (`WildMagicCard` / `WildMagicDraw`): вытягивание/выбор карт.
- ⬜ Классовые бонусы (`ClassBonusRecord`): пороги, бросок кубов, фиксация эффекта.
- ⬜ Валюта и история транзакций (`Currency` / `CurrencyTransaction`) — экран баланса с обязательным `moneyTarget`.
- ⬜ Просмотр аудит-лога (`AuditLog`).
- ⬜ Бросок кубов: формулы боя уже принимают готовые значения дайсов — добавить роллер + историю бросков.

### 7. PWA и офлайн 🚧
Детальный план первой итерации («установка + офлайн-оболочка») вынесен в `MOBILE-APP.md`.
- ✅ PWA-каркас (Serwist): `app/manifest.ts`, service worker `app/sw.ts` (→ `public/sw.js`, в dev выключен), иконки 192/512/maskable + favicon/apple-touch из `icon_src.png` (`scripts/generate-icons.mjs`), офлайн-фолбэк `/~offline`, iOS-мета. Навигации/данные — `NetworkOnly` (не отдаём устаревшие значения из кэша). Собрано и проверено отдачей роутов; установка на устройство и Lighthouse — на VM/телефоне.
- ⬜ Офлайн-чтение данных персонажа и клиентское кэширование справочников — будущая итерация.

### 8. Прод-эксплуатация и наблюдаемость ⬜
- ⬜ Бэкап БД (cron) и стратегия восстановления.
- ⬜ Логирование ошибок (Sentry или аналог), базовые метрики.
- ⬜ Rate limiting на публичные эндпоинты после появления API.

### 9. Telegram-бот для игрока 🚧 — приоритет
Бот как ещё один потребитель игрового ядра (`@gob/rules` + `@gob/db`) — **путь A**: отдельное
приложение монорепо `apps/bot` ходит к БД напрямую, без отдельного публичного API (трек п.2
этим не блокируется и не отменяется). Технология — grammY, long-polling для старта.
Все инварианты движка (§ «Золотые правила» в `CLAUDE.md`) обязательны: математика только из
`@gob/rules`, никакого clamp/блокирующей валидации (правило 1), права по `ownerId` (правило 6).

- 🚧 **`packages/core` (`@gob/services`)** — вынести оркестровку мутаций из server actions
  (`apps/web/src/actions/*`): проверки владения `ownerId`, запись `AuditLog`,
  `CurrencyTransaction` с обязательным `moneyTarget`, работа с парами
  `*Computed`/`*Override`/`*ManualOverride`. Веб-экшены становятся тонкими обёртками
  (`auth()` + `revalidatePath()` вокруг вызова core); бот вызывает тот же слой. Рефакторинг
  без смены поведения — тесты остаются зелёными. _(Общий шаг с п.2: единый источник логики.)_
  - ✅ Каркас пакета `@gob/core` + `Actor`/`canEditCharacter` (проверка владения, § правило 6,
    покрыта юнит-тестом), `loadRuleConfig` (переехал из `apps/web/src/lib/rule-config.ts`,
    путь сохранён реэкспортом), `updateRuntimeCurrentValues` (текущие HP/мана/ОД/сытость/бабл,
    без clamp — § правило 1). Веб-экшен `updateRuntimeValues` переключён на ядро.
  - ✅ Telegram-оркестровка в ядре: `telegram.ts` (генерация/погашение кода привязки,
    `resolveActorByChatId`, статус/отвязка; чистые `createLinkCode`/`isLinkCodeExpired`
    покрыты тестами) + read-model `character-summary.ts` (`getActorCharacterSummary` —
    производные через `@gob/rules`, текущие значения без clamp). `apps/web` подключён к ядру
    (`transpilePackages` + dep); экшены `profile.ts` для привязки — тонкие обёртки над core.
  - ⬜ Перенести остальную оркестровку: характеристики, оверрайды производных, валюта
    (`CurrencyTransaction`), репутация, снаряжение/рюкзак, скиллы.
- ✅ **Привязка аккаунта** — таблица `TelegramLink` (`accountId`, `telegramChatId`,
  одноразовый `linkCode` + `linkCodeExpiresAt`; soft-delete по конвенции). Cookie-сессия для
  бота не годится. Поток: игрок генерирует код в `/settings` → шлёт боту `/start <код>` → бот
  связывает `chat_id` с аккаунтом и дальше знает роль/`ownerId`.
  - ✅ Prisma-модель `TelegramLink` + связь на `Account` + миграция
    `20260627120000_add_telegram_link` (SQL вручную; применится `migrate deploy` на VM).
  - ✅ Генерация/перевыпуск кода в `/settings` (секция «Telegram-бот», статус + код + deep-link
    `t.me/<bot>?start=<код>` при заданном `NEXT_PUBLIC_TELEGRAM_BOT_USERNAME`, отвязка),
    `resolveActorByChatId`/`consumeLinkCode` в `@gob/core`, хэндлер `/start <код>` в боте
    (TTL кода — 15 мин, освобождение chatId от прежней привязки в транзакции).
- ✅ **Скелет `apps/bot`** — grammY + long-polling, `BOT_TOKEN` из env (Zod-валидация,
  `dotenv` подхватывает `apps/bot/.env`), HTTP-healthcheck `GET /health`
  (`BOT_HEALTHCHECK_PORT`, по умолчанию 3001 — у long-polling своего порта нет), команды-
  заглушки `/start` и `/ping`, корректное завершение по SIGINT/SIGTERM. Запуск через `tsx`
  (`dev`=watch / `start`), как сиды `@gob/db`. Подключён в pnpm-workspace (`apps/*`) и turbo
  (lint/typecheck/test зелёные на всех 6/6 пакетах). Плейсхолдер `BOT_TOKEN` в `.env.example`;
  реальный токен — только в gitignore-`.env`. Проверено локально: бот вышел на связь как
  `@GameOfBrazaBot`, `/health` → `200 {"status":"ok"}`. README — `apps/bot/README.md`.
- 🚧 **MVP-команды игрока**:
  - ✅ `/me` — сводка листа (имя/раса, HP/мана/ОД `cur/max` с эффективным максимумом, сытость,
    характеристики). Максимумы через `@gob/rules`; выход за рамки (оверхил/отрицательные) —
    нейтральная пометка «вне расчётных рамок», а не ошибка (§ правило 1). Формат — чистая
    функция `apps/bot/src/format.ts` (покрыта тестами).
  - ✅ `/hp ±N` `/ap ±N` `/mana ±N` — правка живого значения на дельту (atomic `increment`,
    **без clamp** — оверхил/отрицательные допустимы, § правило 1) + инлайн-кнопки −5/−1/+1/+5
    (callback `adj:<resource>:<delta>`, тот же путь). Без аргумента — показ текущего значения
    с кнопками. Оркестровка в `@gob/core` `adjustRuntimeResource` (без аудита, как веб
    `updateRuntimeValues`). Парсинг дельты — чистая `apps/bot/src/parse.ts` (покрыта тестами).
    Меню команд через `setMyCommands`.
  - ✅ `/bag` — инвентарь: надетое снаряжение (экземпляры `equipped_*`, имя/тир как override
    поверх шаблона) + рюкзак (free-text `BackpackSlot` + экземпляры `location=backpack`).
    Только чтение, без правил. Read-model `getActorInventory` в `@gob/core`, чистый
    `formatInventory` в боте (покрыт тестами).
  - ✅ `/money` — баланс активного листа: золото/серебро/бронза через `formatCurrency`
    (`@gob/rules`, курсы из `RuleConfig`) + нормализованный итог в бронзе. Read-model
    `getActorMoney` в `@gob/core`, чистый `formatMoney` в боте (покрыт тестами). Общий
    where-фильтр активного листа вынесен в `activeCharacterWhere` (`/me`/`/hp`/`/bag`/`/money`
    смотрят на один и тот же персонаж).
  - ✅ `/roll` — броски кубов. Структура: `/roll <обозначение> <кубы>` либо просто `<кубы>`
    (`NdM`/`dM`/`M`, есть и кириллическая `д`). Виды: `STR/DEX/INT/SPI/END/LUC` — проверка
    характеристики (1=крит-успех, ≤стат=успех, макс грань=крит-провал; меньше лучше); `ATK` —
    попадание (`≥dieFaces−1−DEX`) + крит (`≥dieFaces−критМод`, критМод=⌊LUC/lucCritStep⌋; крит без
    попадания = критпромах); `DMG` — сумма кубов + плоский доп. урон от характеристики надетого
    оружия (`scalingAttribute×scalingCoefficient` из карточки, не входит в сумму); `BBL` — d100,
    мутирует состояние бабла (порог `min(100,заряды×bubbleChargePercent)`, спал при `d100≥порога`
    → заряды 0; устоял → заряды−1 мин 1; индикатор «дух ≥ 6 → не остаётся на 0»). При нескольких
    кубах выделяются макс 🔺 / мин 🔻 (преимущество/помеха игрок берёт сам). Обычный бросок —
    без привязки аккаунта. Математика — `@gob/rules` (`dice`/`combat`/`bubble`), RNG — в боте
    (`roll.ts`), оркестровка — `@gob/core` (`getActorRollContext`/`applyActorBubbleRoll`), формат —
    чистый `format.ts`; всё покрыто тестами. _Заметки для будущей сверки с заказчиком:_ крит-порог
    взят как `dieFaces−критМод` (база = натуральный максимум, ТЗ §3.4), а не дословный `−1−критМод`;
    граница падения бабла `≥` (как описал заказчик) расходится с ТЗ §3.7 (`>`) — при желании свести
    `resolveBubbleHit`/`resolveBubbleRoll` к одной форме и обновить §3.7.
    - ⬜ **`/rollm` — ручной ввод бросков.** Та же структура аргументов, что у `/roll`
      (`/rollm <обозначение> <значение>`), но вместо генерации RNG принимает готовое
      число от игрока (результат реального физического броска). Применяет те же
      интерпретации (STR/DEX/.../ATK/DMG/BBL), мутирует состояние бабла при BBL — только
      источник результата иной. Полезно для переноса бросков «из реального мира» в систему,
      когда игрок бросил физический кубик за столом. Реализация: парсинг числа вместо
      `parseDiceSpec`; далее та же ветка формата и оркестрации, что у `/roll`.
    - 🚧 **Антикрит на натуральной 1 в `/roll ATK`** (ТЗ §3.4 «возможно попадание по союзнику»).
      Сделано: при натуральной 1 вывод помечает «⚡ Антикрит — попадание по союзнику» (перебивает
      обычный исход) + подсказка кинуть урон по союзнику как обычное попадание, без крит-кубов.
      ⬜ На будущее: автоматически бросать кубы урона оружия по союзнику прямо в `/roll ATK`
      (урон как при обычном попадании по врагу, `crit_multiplier`/`bonus_crit_dice` не применяются)
      — потребует читать `damageDice`/scaling оружия в `getActorRollContext` и связать ATK с уроном.
- ⬜ **Деплой** — сервис `bot` в `docker-compose.yml` рядом с `web` (та же БД);
  `BOT_TOKEN` в `.env.example` / `.env.production.example` (плейсхолдер, без секрета в репо).
- ⬜ _Будущее:_ webhook вместо long-polling за прокси; команды ГМ; пуш-уведомления о событиях.


### 10. Сравнение и интеграция с альтернативным проектом

Изучен альтернативный проект (`C:\GameOfBrothers`): Vanilla JS + Express + localStorage, без фреймворка и БД. Элегантный proof-of-concept с красивым UI, но без сложных механик (нет бабла, классов, репутации, валюты). Формулы жёстко в коде, нет типизации и тестов.

**UI-идеи для заимствования** (не требуют архитектурных изменений):
- ⬜ **3D-карусель** персонажей на CSS `rotateY` + `translateZ` — для экрана списка.
- ⬜ **Page-flip анимация** в спеллбуке (CSS flip 850мс, две страницы-разворота) — для вкладки скиллов/гримуара.
- ⬜ **Римские цифры** для уровней скиллов (`I`–`V`) в `tab-skills`.
- ⬜ **Smart tooltip-позиционирование** (избегает краёв экрана) — полезно на мобильных.

**Интеграция (экспорт/импорт JSON):**
- ✅ **Согласовать схему обмена**: `schemaVersion:1`, `source:"GameOfBraza"`, поля `name/description/lore/stats/combat/equipment/backpack/spells`. Zod-схема в `apps/web/src/lib/gob-brothers-compat.ts`.
- ✅ **Модуль экспорта** — `exportToGoBrothers(character, ruleConfig)` в `gob-brothers-compat.ts`; кнопка «Перенос ↕» в хедере листа персонажа открывает модалку с JSON-textarea + «Скачать .json». Математика через `computeDerived` + override-пары.
- ✅ **Модуль импорта** — `parseGoBImport` + Server Action `importCharacterFromJSON` в `apps/web/src/actions/transfer.ts`; создаёт нового персонажа из JSON (характеристики, рантайм с bonusOverride, снаряжение как free-text `ItemInstance`, рюкзак как `BackpackSlot`). Спеллы и нераспознанное описание идут в `playerNotes`.
- ⬜ `AuditLog`-запись при импорте (сейчас не пишется).

---

## 💡 Идеи к обсуждению
- Экспорт/импорт персонажа (JSON) и шаринг листа по ссылке (read-only).
- Англоязычная локаль (`en`) — структура i18n уже готова.
- Realtime-синхронизация листа во время игры (WebSocket/SSE) — несколько устройств за столом.
- Калькулятор боя на основе `@gob/rules` как отдельный режим для ГМ.
